exports.performSeriousCalculations = function (x,y) {
  //
  // Perform very serious calculations on very important data.
  //
  return y * x;
};

exports.performSeriousCalculationss = suma (x,y) {
  //
  // Perform very serious calculations on very important data.
  //
  return x + y;
};


